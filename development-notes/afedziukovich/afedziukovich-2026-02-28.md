# Дата: 2026-02-22

- **Что было сделано:**
  - Создал компонент шапки `AppHeader.vue` с навигацией и выпадающим меню пользователя.
  - Написал стор авторизации `stores/auth.ts` на Pinia с состояниями и действиями `login`/`logout`.
  - Добавил новые страницы: `about.vue`, `courses.vue`, `dashboard.vue`, `login.vue`, `register.vue`.
  - Интегрировал шапку в `layouts/default.vue`, чтобы она отображалась на всех страницах.
  - Создал отдельный layout `auth.vue` для страниц входа/регистрации.
  - Настроил алиасы импорта и исправил ошибки резолва модулей (использовал `~~/stores/auth`).
  - Подготовил структуру для middleware (папка `middleware/`), но сами middleware пока не реализованы.

- **Проблемы:**
  - ESLint ругался на форматирование (Prettier) и порядок атрибутов в Vue-шаблоне.
  - Импорт стора с одинарной тильдой (`~/stores/auth`) не работал из-за настройки `srcDir`.
  - На странице логина не отображалась шапка, потому что использовался layout `auth`.

- **Решения (или попытки):**
  - Запустил `npx eslint --fix` для автоисправления ошибок форматирования.
  - Заменил импорт на `~~/stores/auth` (двойная тильда), чтобы гарантированно указывать на корень проекта.
  - Временно убрал `definePageMeta({ layout: 'auth' })` со страницы логина, чтобы она использовала `default` layout и шапка появилась. Позже можно будет вернуть отдельный layout и добавить шапку в него при необходимости.
  - Добавил в `vitest.config.ts` плагин `vite-tsconfig-paths` для поддержки алиасов в тестах.

- **Мысли / Планы:**
  - Реализовать middleware для защиты маршрутов `/dashboard` и `/profile`.
  - Доработать страницы `dashboard.vue`, `profile.vue` и `library.vue`.

- **Затраченное время:** ~8 часов